// Placeholder widget test — the default counter test was replaced because
// MyApp initializes Firebase and requires environment setup. Use the
// feature-specific tests in test/models/ and test/providers/ instead.

void main() {
  // No widget-level smoke tests here; integration tests cover app startup.
}
